import { Container } from "dockerode";
import { ExecResult } from "./types";
import { DockerContainerClient } from "./docker-container-client";
export declare class PodmanContainerClient extends DockerContainerClient {
    exec(container: Container, command: string[], opts?: {
        log: boolean;
    }): Promise<ExecResult>;
}
